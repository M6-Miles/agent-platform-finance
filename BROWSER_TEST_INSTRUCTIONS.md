# 浏览器验收测试说明

## 前提条件

- ✅ 后端服务已启动在 http://127.0.0.1:8001
- ✅ 前端页面已通过 `start frontend_prototype.html` 在浏览器中打开（file:// 协议）
- ✅ 所有 602 个单元测试已通过
- ⚠️ 注意：前端通过本地文件打开，需要手动配置后端 API 地址为 `http://127.0.0.1:8001`

## 测试步骤

### 测试 1：DEMO001 离线模式（全部离线样例数据）

1. **创建新会话**
   - 点击"新建会话"按钮
   - 输入会话名称（例如："DEMO001离线测试"）
   - 确认创建

2. **选择数据源**
   - 在左侧边栏找到"数据源选择"
   - 选择 **"离线样例"** 单选按钮

3. **启动深度投研**
   - 在"深度投研"区域输入：`DEMO001`
   - 点击"启动投研"按钮
   - 等待 4 个 Agent 完成分析（约 10-30 秒）

4. **验证数据状态徽标**
   
   期望结果：所有 4 个分析卡片都应显示 **🔵 离线样例** 徽标
   
   - ✅ 技术分析：显示 `🔵 离线样例`
   - ✅ 基本面分析：显示 `🔵 离线样例`
   - ✅ 行业分析：显示 `🔵 离线样例`
   - ✅ 市场状态：显示 `🔵 离线样例`

5. **验证降级原因**
   
   期望结果：所有徽标悬停时应显示 "数据来源: 内置样例数据（离线模式）"，无降级警告

### 测试 2：600519 自动模式（真实数据或降级）

1. **创建新会话**
   - 点击"新建会话"按钮
   - 输入会话名称（例如："600519自动测试"）

2. **选择数据源**
   - 在左侧边栏选择 **"自动（优先实时）"** 单选按钮

3. **启动深度投研**
   - 输入：`600519`（贵州茅台）
   - 点击"启动投研"按钮
   - 等待分析完成

4. **验证数据状态徽标（预期混合状态）**
   
   可能的结果（取决于 AkShare 可用性）：
   
   **场景 A：AkShare 完全可用**
   - ✅ 所有 4 个卡片显示 `🟢 实时数据`
   
   **场景 B：AkShare 部分可用**
   - 某些卡片显示 `🟢 实时数据`
   - 某些卡片显示 `🟠 已降级` 或 `🔴 数据不可用`
   - 悬停时应显示具体降级原因（例如："真实数据源失败: TimeoutError"）
   
   **场景 C：AkShare 不可用**
   - 可能降级为 `🟠 已降级` 或显示错误

5. **验证综合研判数据完整度警告**
   
   如果有任何非"实时数据"状态，综合研判区域应显示：
   ```
   ⚠️ 数据完整度提示：本次分析使用了 [N] 个离线/降级数据源
   ```

### 测试 3：实时行情接口（单一标的）

1. **在浏览器控制台执行**
   ```javascript
   fetch('/quote/600519')
     .then(r => r.json())
     .then(d => console.log('Quote data_status:', d.data_status, d.fallback_reason))
   ```

2. **期望结果**
   - 如果 AkShare 可用：`data_status: "live"`, `fallback_reason: null`
   - 如果 AkShare 不可用：`data_status: "offline_sample"` 或 `"fallback"`，并有 `fallback_reason` 说明

## 验收标准

- ✅ **标准 1**：DEMO001 离线模式下，所有 4 个徽标都是 🔵 离线样例
- ✅ **标准 2**：徽标悬停显示正确的数据来源和降级原因
- ✅ **标准 3**：综合研判在有降级数据时显示完整度警告
- ✅ **标准 4**：数据状态徽标颜色准确反映数据质量（绿>蓝>橙>红）
- ✅ **标准 5**：页面无 JavaScript 错误（检查浏览器控制台）

## 已知限制

- **000001（平安银行）**：样例数据仅包含基本面和行业数据，技术分析会失败（预期行为）
- **DEMO 代码**：仅在离线模式或 `/research` 端点有效，`/quote` 端点会拒绝
- **工作流中断**：当持仓超过风险限制时，工作流会在 `manual_review` 暂停（预期行为）

## API 验证（已完成）

```bash
# 已验证 - 所有 4 个 agent 返回 data_status 字段
curl -s "http://127.0.0.1:8001/research/1fa8da9fa5d444b3/state" | python -c "..."
# 输出：
# Technical: offline_sample
# Fundamental: offline_sample
# Industry: offline_sample
# Market: offline_sample
```

## 下一步

完成浏览器验收后，继续执行 Plan 中的文档生成任务。
