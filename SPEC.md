# SPEC.md — 项目成功标准

> Agent = Model + Harness · V2.0

## 1. 平台层（Harness 主线）

| 编号 | 验收标准 | 状态 |
|------|----------|------|
| P-01 | Harness 九大组件齐全（SPEC / Rule / Skill / Workflow / Scripts / MCP / SubAgent / dev-map / 看板） | ✅ |
| P-02 | `AgentHarness` SDK 可运行，支持 Guardrail 插件化注册 | ✅ |
| P-03 | Loop 五要素 + 记忆层可运行、可持久化 | ✅（2026-08-09 补齐：规划/反思产物、心跳、事件钩子、目标循环，见 §1.2） |
| P-04 | Graph 引擎支持并行调度、条件边、Checkpoint 断点恢复 | ✅（2026-08-07 升级：LangGraph 成为主引擎，见 §1.1） |
| P-05 | 非金融 demo 可在 ≤ 2 天内接入 | ✅ |

### 1.1 P-04 升级：LangGraph 迁移（2026-08-07）

证券分析主工作流已完整迁移至 LangGraph `StateGraph`（langgraph 1.2.10，Python 3.11.7）。

| 项目 | 当前实现 |
|------|----------|
| 编排引擎 | `finance/securities_graph.py`（LangGraph StateGraph） |
| 并行调度 | LangGraph 多边并行超步 |
| Checkpoint | 默认 `SqliteSaver`；`MemorySaver` 仅限显式开发配置 |
| 人工审批 | `interrupt()` 暂停 + `Command(resume=...)` 恢复（`securities_graph.py:434 / 471 / 664`） |
| 状态模型 | `SecuritiesAnalysisState` TypedDict；`errors` / `trace_entries` 用 `Annotated[list, operator.add]` reducer（`securities_graph.py:95 / 101`） |
| 服务入口 | `application_service.deep_research` → `run_securities_analysis` |
| 测试覆盖 | LangGraph 工作流、审批恢复、离线网络封锁与 API 集成测试 |

**工作流节点**（12 个，不含 START/END）：

```
technical_agent, fundamental_agent, industry_agent, market_regime_agent
→ synthesis_agent → debate_approval / no_trade / trader_agent → human_approval
→ risk_manager → evaluator_agent → trading_harness
```

**条件路由**：
- `synthesis_agent` 后：置信度 > 0.3 → `trader_agent`；否则 → `no_trade` → END
- `trader_agent` 后：`HumanApprovalRequired` → `human_approval`；正常 → `risk_manager`；错误 → END
- `human_approval` 后：approve → `risk_manager`；reject → END（block）
- `risk_manager` 后：固定进入 `evaluator_agent`，独立评估 Synthesis、Trader 与 Risk Manager 输出
- `evaluator_agent` 后：进入 `trading_harness`；最低质量分 < 80 时 Pre-Flight 强制人工复核
- `trading_harness` 后：execute → END；block → END；manual_review → `human_approval`（interrupt）

**当前状态**：证券分析 API、离线验收和人工审批恢复均使用 LangGraph；旧 GraphEngine 和历史 YAML 工作流已移除。

### 1.2 P-03 补齐：Loop 五要素显式化 + 心跳 / 事件钩子（2026-08-09）

**补齐前的真实状态（本次审计发现，此前 SPEC 标 ✅ 属高估）**：
`core/agent_runtime.py` 仅 69 行，是一个裸 ReAct 循环 —— 有工具调用与观察，
「继续/结束」隐含在 `not reply.tool_calls` 与 `max_steps` 里，但**没有**显式的
规划产物、反思产物，也**完全没有**心跳/定时、事件钩子、目标循环
（`grep heartbeat|event_hook` 在 `src/` 与 `tests/` 下零命中）。
记忆持久化那部分是真实的（`storage/sqlite_store.py` 的 `add_message` /
`list_messages` + LangGraph checkpoint），未夸大。

**本次新增的四个模块**（均为项目内可运行、可测实现，非文档或空接口）：

| 要素 | 实现 | 说明 |
|------|------|------|
| 心跳 / 定时 | `core/scheduler.py` | `HeartbeatScheduler` + 可注入 `Clock`；`ManualClock` 使定时测试确定性化，不靠 `time.sleep`。单个定时器回调异常不拖垮调度器，失败计入 `stats()['failed']`；阻塞错过的周期只补触发一次并记入 `skipped_beats`，不做补偿式连发 |
| 事件钩子 | `core/event_hooks.py` | `EventBus` + `LoopEvent` 十个标准事件名；支持优先级、`once`、注销；钩子异常被捕获记录但**绝不外泄**到主流程，失败可由 `bus.failures()` 断言 |
| 记忆（可持久化） | `core/loop_memory.py` | `LoopMemory` 协议 + `InMemoryLoopMemory`（显式 `is_persistent() == False`）+ `SQLiteLoopMemory`。存**推理产物**（规划/观察/反思/决策），使用独立 `loop_memory` 表，不改动既有 `chat_messages` schema |
| 规划 / 反思 / 目标循环 | `core/agent_loop.py` | `AgentLoop.run()` 每轮依次产出五要素并全部落盘 + 广播；`RuleBasedPlanner` 与 `KeywordReflector` 为确定性实现，`provider=None` 时离线禁网可跑 |

**验收证据**：

| 项目 | 结果 |
|------|------|
| 专项测试 | `tests/test_scheduler.py` 39 · `tests/test_event_hooks.py` 38 · `tests/test_loop_memory.py` 49 · `tests/test_agent_loop.py` 60 = **186 项全过** |
| 可运行入口 | `Scripts/run_agent_loop_demo.py`（离线，退出码 0） |
| 五要素齐备 | 记忆中 plan/tool_call/observation/reflection/decision + goal/answer 七类均非空 |
| 目标循环多轮 | 第 1 轮补齐 2 项证据、判定仍缺 `regime` → 第 2 轮补齐 → `stop_reason=goal_met` |
| 持久化 | 换实例指向同一 db 文件读回 14 条（判定标准是跨实例读回，不是「内存字典非空」） |
| 零网络 | 仅调用 3 个 `requires_network=False` 工具，`blocked_offline=0` |
| 变异测试 | 4 处人为缺陷全部被测试抓到（详见 §1.2.1） |

#### 1.2.1 变异测试（证明测试真的会报警）

| 注入的缺陷 | 被抓到的测试 |
|-----------|-------------|
| 心跳回调异常被伪装成成功（`ok=False` → `ok=True`） | `test_scheduler.py::TestFailureIsolation::test_failing_callback_does_not_block_others` |
| 钩子异常被伪装成成功 | `test_event_hooks.py::TestFailureIsolation::test_failing_handler_does_not_stop_others` |
| SQLite 记忆不提交事务（假持久化） | `test_loop_memory.py::TestReadWrite::test_records_returns_in_insertion_order[sqlite]` |
| 反思器把失败工具的报错文本当成证据（去掉 `if o.ok`） | `test_agent_loop.py::TestFailuresAreVisible::test_failed_output_containing_keyword_does_not_meet_goal` |

四处变异均使退出码变为 1，还原后恢复 0，无残留 `.bak` / `.orig`。

**顺带修复的真实缺陷**：`SQLiteLoopMemory` 原先用 `with self._connect()`，
该写法只提交事务、**不关闭连接**，文件句柄要等 GC 才释放，在 Windows 上导致
「写完记忆后删不掉 db 文件」（WinError 32），长跑的 Loop 还会累积句柄。
改为 `_session()` 上下文管理器（`with connection` 负责提交/回滚，`finally`
负责 `close()`）。此缺陷是在 Loop 演示脚本清理临时库失败时暴露的。

**仍未覆盖**：`agent_runtime.py`（既有 ReAct 循环，`ApplicationService.chat()`
在用）未改动，因此**聊天主链路仍是裸 ReAct**，尚未切换到 `AgentLoop`。
本次交付的是「五要素可运行可验收的实现 + 独立入口」，不是「聊天链路已迁移」。

## 2. 应用层

| 编号 | 验收标准 | 状态 |
|------|----------|------|
| A-01 | 技术分析 Agent 输出结构化报告，通过 JSONSchema 校验 | ✅ |
| A-02 | 基本面分析 Agent 输出 PE/PB/ROE/DCF 估值，通过 Schema 校验 | ✅ |
| A-03 | 行业分析 Agent 输出景气度 + 龙头排序 | ✅ |
| A-04 | 大盘/宏观 Agent 输出 Market Regime 判断 | ✅ |
| A-05 | Bull/Bear 辩论 + Synthesis 输出置信度（0–100%） | ✅ |
| A-06 | Trader + Risk Manager 输出可执行交易建议并过风控 | ✅ |
| A-07 | 端到端 Graph 对 ≥ 20 只股票跑通完整投研流程 | ✅ |

## 3. 工程层

| 编号 | 验收标准 | 状态 |
|------|----------|------|
| E-01 | 回测夏普比率 > 0.5（基线，不承诺盈利） | ❌ **未达标**（见 §3.1） |
| E-02 | 可观测面板：Token 消耗、耗时、失败率可视化 | ✅ |
| E-03 | 熔断机制：连续失败 N 次自动暂停 + 告警 | ✅ |
| E-04 | Evaluator Agent 独立质量评分可运行 | ✅（已接入 LangGraph 主链与可观测 trace） |
| E-05 | Harness 效果量化：幻觉率下降 X%，无效调用减少 Y% | ⚠️ **部分达标**（见 §3.2） |
| E-06 | 文档完整，代码可复现 | ✅ |
| E-07 | 模拟盘按真实行情自然运行 1–2 周 | ⚠️ **收集中：1/7 个有效交易日** |

### 3.0.1 外部服务与调度可靠性（2026-08-15）

- `/health/providers` 分别暴露行情、天气 Provider、DeepSeek 状态；启动自检不会调用付费 LLM。
- 行情与天气记录成功率、缓存命中率、P95 延迟、连续失败和下一次重试时间；连续失败采用指数退避。
- 中国境内天气预报优先使用 CMA-GRAPES，超出 CMA 可用时效后由 Open-Meteo Best Match 补全；每个预报日返回并展示模型来源。
- 省级行政区不能直接作为单一天气坐标；用户必须选择具体城市。新疆维吾尔自治区支持预置城市和区县，预置区县缺少精确候选时使用省、市、区联合键约束的行政区中心参考坐标，未知区县才明确回退城市级。
- 证券分析必须校验六位 A 股代码及数据源返回标的的一致性；不存在的代码返回明确错误，不得复用样例或其他股票结果。
- 腾讯行情失败后，仅允许字段一致性校验通过的新浪公开行情作为延迟备用源；两者都失败时使用明确标记时间的最近成功缓存或返回不可用。
- 长期监控使用本地版本化 A 股交易所日历；当前覆盖 2025-01-01 至 2026-12-31，超出范围不冒充权威交易日。
- `(job_id, trading_date)` 唯一约束保证每日幂等，SQLite scheduler lease 保证多后端进程不会重复执行同一轮调度。
- 前端 CSS 与字体均为项目内资源，生产运行不依赖 Tailwind CDN 或 Google Fonts。
- 回测页面以同一行情区间、同一引擎、同一成本同时展示 MA5/MA20 与买入并持有；未修改 Sharpe 公式和 0.5 阈值。
- `deploy/` 提供单机 Docker、Nginx、可选 HTTPS、SQLite 持久化与每日备份；版本更新保留服务器 `.env.production` 和 `data/`。2026-08-15 已在 Ubuntu 服务器完成构建、容器健康、`/health`、`/ready`、Nginx 和页面功能验收。

### 3.2 E-05 真实 LLM 实验口径（2026-08-14）

新版 100 条 DeepSeek 回放已完成：标签匹配率 91.0%，违规拦截召回率 100%，
正常请求误报率 0%，Provider 错误率 0%。40 条固定事实样本本轮未观测到事实错误，
因此真实“事实错误阻断率/下降率”分母为 0，必须记为 N/A，不能用 0% 幻觉率推导
Harness 降幅。反事实下游动作资格由 Harness OFF 的 60 次降至 ON 的 0 次，降幅 100%；
该指标不代表实际调用交易接口。权威报告为
`docs/experiments/real_llm_replay_deepseek_latest.md`。

### 3.1 E-01 未达标说明（2026-08-05 实测，2026-08-06 十年窗口复测）

回测**引擎**本身可用（Sharpe / 最大回撤 / 胜率 / 滑点 0.1% / 佣金 0.03% 均已实现并被
`tests/` 覆盖）。未达标的是「夏普 > 0.5」这一**策略绩效指标**：

| 数据 | 信号路径 | 逐股平均 Sharpe | 达标数 | 等权组合 Sharpe |
|------|---------|----------------|--------|----------------|
| 真实 A 股 20 只<br>（AkShare 前复权，2025-08-05 ~ 2026-08-04） | MA5/MA20 金叉死叉 | **−0.440** | 4/20 | +0.073 |
| 同上 | 平台 `synthesize()` 周频 | **−0.534** | 5/20 | −0.086 |
| 确定性合成集 TEST001-020 | MA5/MA20 金叉死叉 | **+0.33** | 9/20 | — |
| **真实 A 股 20 只**<br>（前复权，2016-08-05 ~ 2026-08-04，2415 日/股） | 平台 `synthesize()` 周频 | **+0.254**（日历口径）<br>+0.429（持仓口径） | 5/20 | **+0.593** |

> 上表最后一行的等权组合值在 2026-08-06 由 **+0.811 更正为 +0.593**，逐股均值口径同时
> 更正。原因是脚本的组合构造按行序对齐而非按日期对齐（见 §3.1.3），不是策略变化。

#### 3.1.1 十年窗口复测（2026-08-06）：点估计转正，但仍不能宣称达标

1 年窗口下年化夏普标准误 ≈ 1.00，实测 −0.440 的 95% CI 约 [−2.4, +1.5] 把目标 +0.5
整个包住 —— **该口径下 E-01 在统计上不可检验**，无法区分「策略确实不行」与「样本太短」。
故延长至约 9.6 年（SE 1.00 → **0.323**），使命题可证伪。结果：

```
等权组合   Sharpe = +0.593   SE = 0.322   95% CI [−0.039, +1.225]
判定：无法区分于 0.5（CI 覆盖该值），且 CI 下界 < 0 ⇒ 亦无法区分于 0
买入持有基准（无成本）：+0.412
逐股均值（日历口径）+0.254（5/20 点估计达标）   均值最大回撤 43.52%   均值交易数 46.8
```

**E-01 仍记为未达标。** 点估计 +0.593 略高于目标，但 95% CI 覆盖 0.5，不构成「显著高于
0.5」；20 只逐股中 19 只判定为「无法区分」，1 只（600009）显著低于 0.5。按本节末尾的
禁令，不得以点估计代替显著性宣称通过。

进一步地，修正对齐缺陷后 CI 下界为 **−0.039 < 0**，因此**连「显著优于零」也不成立**。
（2026-08-06 早先版本据 +0.811 写过「显著优于零」，该结论随对齐修正一并失效，见 §3.1.3。）
逐股均值日历口径 **+0.254 低于买入持有基准 +0.412** —— 即在单股层面，该管线在这十年上
并未跑赢什么都不做。

**四项限制，均不可通过调参消除：**

1. **样本外不存活（最关键）**：严格切分训练段 2016-08~2021-12 / 留出段 2022-01~2026-08，
   留出段不参与任何判断。训练段 +0.986，留出段 **−0.035**，衰减 **−1.022**；留出段
   20 只中 0 只点估计达标、8 只为负。这是比「夏普差一点」严重得多的证据。
2. **滚动窗口不持续**：3 年窗口步进 1 年共 10 个窗口，仅 **2/10** 的 CI 下界 > 0
   （且均在 2016–2020 段），**0/10** 的 CI 下界 > 0.5，2 个点估计为负。
   全期点估计由前半段贡献，不构成「稳定边际」。
3. **存活者偏差（2026-08-06 已量化，见 §3.1.4）**：20 只均为至今在市公司，退市/暴雷
   标的被结构性排除。已实测 225 只同期退市标的：逐股均值 **−0.475**（vs 存活池 +0.254，
   差 **−0.729**），84.4% 为负。按真实死亡率（≈7.6%）重加权后，等权组合 +0.593 降至
   **+0.44 ~ +0.19**（取决于退市清算损失假设）。该修正**不改变 E-01 的未达标结论**，
   且使差距更大而非更小。留出段结论同样偏乐观。
4. **逐股夏普存在两个口径**：引擎的 `sharpe_ratio` 只统计持仓日收益却按 sqrt(252)
   年化（见 §3.1.2），时间在市约 48.7%，逐股均值 +0.429（持仓口径）对应日历口径
   **+0.254**。现已在引擎中并列输出两个口径，达标判定一律用日历口径。

#### 3.1.2 复测中发现并修复的引擎缺陷（2026-08-06）

`backtesting.py` 的 `equity_curve` 仅在 `in_position` 为真时追加，长度是**持仓天数**
而非交易日数（实测 1280 vs 2397）。已改为按日历对齐（空仓日追加重复净值）。
`daily_returns` / `equity` / `sharpe` / `max_dd` 逐位不变 —— 空仓日的重复值不可能
造出新的峰或谷，故未静默改写引擎口径。修复后 3 只样本的组合夏普由 +0.561 降至
+0.305，逐年表由截断在 2021 变为完整至 2026。**该修复使数字变差，属修正而非优化。**

引擎仍存一处口径不自洽（未修，仅记录）：`annualized_return_pct` 按日历天年化，
而 `ann_vol` / `sharpe` 按持仓天年化，后者系统性放大夏普约 1/sqrt(时间在市) ≈ 1.37 倍。

**历史上记录的 0.72 不成立**，原因有两处，均已修正：

1. 合成数据生成器写死了 `random.gauss(0.0008, vol)`，即每日 +0.08% 正漂移（约年化
   +22%）。单边做多策略跑在预设上涨的序列上，夏普为正是构造出来的。
2. 播种用 `random.seed(hash(symbol))`，而 CPython 对 `str` 的 `hash()` 默认加盐
   （`PYTHONHASHSEED` 随进程随机）。0.72 是当时那份已落盘 CSV 的一次抽样结果，
   换种子即不复现 —— 这同时违反 E-06 的「代码可复现」。

现生成器（`Scripts/generate_sample_data.py`）改用固定整数种子，漂移按标的正负兼有、
量级贴近真实 A 股，不再人为拉高信噪比。

#### 3.1.3 组合对齐缺陷与 +0.811 → +0.593 的更正（2026-08-06）

`Scripts/measure_real_10y.py` 的 `run_arm()` 在构造等权组合时，按**行序**（第 t 项对
第 t 项）对各标的日收益取均值。但 20 只标的的交易日历并不相同 —— 9 只因停牌而更短，
最多缺 74 个交易日（`601088` 缺 74，`000651` 缺 42，`000333` 缺 40）。按行序平均会把
**不同日期**的收益混在一起，等于人为构造了一条无人能持有的净值曲线。

在同一份回测输出上同时用两种对齐方式计算，差异可复现：

```
按行序对齐   n=2425   Sharpe = +0.8112     ← 此前报告的数字
按日期对齐   n=2425   Sharpe = +0.5926     ← 更正后
差            +0.2186
```

即此前报告的 +0.811 中约 **0.219 来自对齐错误**，而非策略表现。已改为按日期对齐
（某日只对当日有数据的标的取均值），并同时返回 `portfolio_dates`，使逐年归因不再
依赖对下标与日期对应关系的推断。

连带失效的结论：**「十年窗口下显著优于零」不再成立**（更正后 CI 下界 −0.039 < 0）。
该结论此前写在 §3.1.1，现已改写。

`Scripts/measure_optimizations.py` 不受影响 —— 它只对逐股标量取均值，从不构造跨标的
日序列。缺陷仅存在于 `measure_real_10y.run_arm`。

#### 3.1.4 存活者偏差的量化（2026-08-06）

**限制③** 说「退市/暴雷标的被结构性排除，故所有数字对真实可实现收益**偏乐观**」，但
此前没给出量级。`Scripts/measure_survivorship_bias.py` 拉取同一窗口（2016-08-05 ~
2026-08-04）内全部退市标的的十年日线（229 只，去重、排除 B 股），按相同策略回测，
与 20 只存活标的的结果并列，得到偏差的方向与量级估计：

```
存活池（20 只）     日历口径逐股均值 +0.254   等权组合 +0.593   95%CI [−0.039, +1.225]
退市池（225 只）    日历口径逐股均值 −0.475   等权组合 −1.018   95%CI [−1.655, −0.381]
逐股均值差（退市 − 存活）  −0.729
等权组合差（退市 − 存活）  −1.611
```

**退市池中 84.4% 为负夏普，存活池中仅 20% 为负** —— 只看活下来的，等于系统性地把左尾
从样本里删掉。最差 10 只：退市吉恩 −4.978、退市昆机 −3.945、德奥退 −3.554、康得退 −3.107、
长生退 −2.768、烯碳退 −2.596、龙力退 −2.335、金亚退 −2.174、中弘退 −1.904、暴风退 −1.841。

**关键：样本构成失真。** 存活 20 只 + 退市 225 只的等权合并池里，死者权重高达 92%，而
真实 2016 年可选池约 3000 只 vs 同期 229 只退市，死亡率仅约 **7.6%**。等权合并会让偏差
被结构性高估。因此脚本分层重加权：

```
按真实死亡率 w=7.6%、清算损失 0%/50%/100% 三档，偏差估计范围：
    +0.445 / +0.321 / +0.193    （即：存活池 +0.593 降至该区间）
```

清算损失 = 退市当日仍持仓者承担的跳空损失，0% 为最乐观假设（按最后收盘价原价了结），
100% 为归零。三档并列以显示结论对该假设的依赖程度。

**本轮在该脚本中发现并修复的第二处日期对齐缺陷**（与 §3.1.3 同类）：`apply_liquidation()`
原将清算日追加为「最后交易日 + 1 自然日」，该日常落在周末/节假日，此时它在合并池里
**只有这一只标的在场**，`portfolio_from` 等权取均值即 n=1，于是一只标的的 −100% 清算
被当成**整个组合**的 −100%。实测 225 只中 37 只（16.4%）踩中此条。症状是清算损失 100%
一档的夏普（−1.621）反而**优于** 50% 一档（−1.688）—— 更坏的假设给出更好的数字，
这个非单调本身就是缺陷的信号。改为将清算日贴到池子交易日并集中最早晚于最后交易日的
那一天后，清算损失正确按 1/n 稀释，三档恢复单调：−1.088 / −1.608 / −1.964。
`tests/test_liquidation.py`（22 条）锁定该语义，含一条直接复现「周五退市、周六无人交易」
的回归用例。

**对 E-01 的影响**：此前报告的等权组合 +0.593 建立在纯存活样本上。按分层重加权估计，
真实可选池的对应值应为 **+0.2 ~ +0.4** 量级（取决于清算损失假设），即 E-01 的实际差距
比已报告的更大，而非更小。这一结论的方向确定，量级受三项局限约束（报告全文见
`data/real/measure_survivorship_result.txt`）：

1. 数据源不同：存活池走新浪 `stock_zh_a_daily`（退市代码 `JSONDecodeError`），退市池
   走腾讯 `stock_zh_a_hist_tx`。前复权基准差异表现为噪声，不构成系统性偏向。
2. 行情源截断部分退市标的历史：4 只不足 60 日（000033/30, 000693/56, 000748/55, 
   300372/42）已剔除，多数退市标的在停牌期不更新行情。
3. 非点位复原：存活池为沪深 300 大市值，退市池为全部退市名单（小盘股占多），
   市值分布不同。故本结果是偏差的**方向与量级估计**，不是精确修正值。

**结论**：单边做多的均线交叉策略在当前样本上不具备**显著** Sharpe > 0.5 的能力。
十年窗口下等权组合点估计 +0.593 略高于目标，但 95% CI [−0.039, +1.225] 同时覆盖 0.5
与 0 —— 既不能宣称达标，也不能宣称优于零。更实质的差距是：**留出段夏普 −0.035**
（衰减 −1.022）、滚动窗口仅 2/10 排除 0、逐股日历口径 +0.254 低于买入持有 +0.412。
要真实达标需改动策略本身（择时 / 风控 / 多因子 / 组合优化），而非调整数据或验收口径。
在此之前 E-01 记为未达标。

**已排除的三条路径**（均经配对检验，不是「没试过」）：追踪止损（十年配对 t=+1.72，
不显著）、regime_aware（t=+2.33「显著」但差值仅 +0.0056，实践无意义）、
sentiment（合成数据上只能测敏感度，无法验证有效性）。三者分别作用于风控 / 权重 /
附加维度，均未改善夏普，共同指向：**瓶颈在信号质量，不在风险管理或权重微调**。

> 不得通过以下方式使 E-01「通过」：向合成数据注入正漂移、挑选有利区间或标的、
> 对参数过拟合。这类做法即本条目要修正的缺陷本身。

## 4. 硬性约束（不得违反）

- **禁止无人工确认下单**：任何仓位 > 10% 的信号必须人工审批
- **数据必须带 source 字段**：所有分析输出必须注明数据来源与时间戳
- **分析仅供参考**：所有输出必须包含"仅供研究参考，不构成投资建议"
- **API Key 不入代码**：DEEPSEEK_API_KEY / ANTHROPIC_API_KEY 只存 `.env`
- **不连接真实券商**：严禁接入实盘交易接口

## 5. 定义"合格的投研报告"

一份合格报告必须包含：
1. 股票基本信息（代码、名称、市场、数据来源、更新时间）
2. 技术面：MA/EMA/MACD/RSI/KDJ/布林带位置/ATR/CCI（代码计算，非 LLM 估算）
3. 基本面：PE/PB/ROE + 估值区间判断
4. 行业面：所属行业景气度
5. 综合研判：Bull/Bear 置信度 + 买卖信号
6. 风险提示：最大回撤 + 免责声明
